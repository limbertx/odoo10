<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="account_report_libroDiario_form" model="ir.ui.view">
        <field name="name">Libro Diario</field>
        <field name="model">report_custom.accountlibrodiarioform</field>
        <field name="arch" type="xml">
            <form string="Opciones de reporte">
                <group col="4">
                    <field name="fecha_ini"/>
                    <field name="fecha_fin"/>
                    <field name="diarios_ids" widget="many2many_tags" options="{'no_create': True}"/>
                </group>
                <footer>
                    <button name="check_report" string="Imprimir" type="object" default_focus="1" class="oe_highlight"/>
                    or
                    <button string="Cancelar" class="oe_link" special="cancel" />
                </footer>
            </form>
        </field>
    </record>

    <record id="action_account_print_libroDiario_menu" model="ir.actions.act_window">
        <field name="name">Libro Diario</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">report_custom.accountlibrodiarioform</field>
        <field name="view_type">form</field>
        <field name="view_mode">form</field>
        <field name="view_id" ref="account_report_libroDiario_form"/>
        <field name="target">new</field>
    </record>

    <!-- adicionamos al menu -->
    <menuitem
        name="Libro Diario"
        id="report_custom.accountlibrodiarioform"
        parent="account.menu_finance_legal_statement"
        groups="account.group_account_manager,account.group_account_user"
        action="action_account_print_libroDiario_menu"/>
</odoo>
