<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="finicio.list">
      <field name="name">finicio list</field>
      <field name="model">finicio.finicio</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="finicio.action_window">
      <field name="name">finicio window</field>
      <field name="res_model">finicio.finicio</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="finicio.action_server">
      <field name="name">finicio server</field>
      <field name="model_id" ref="model_finicio_finicio"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="finicio" id="finicio.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="finicio.menu_1" parent="finicio.menu_root"/>
    <menuitem name="Menu 2" id="finicio.menu_2" parent="finicio.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="finicio.menu_1_list" parent="finicio.menu_1"
              action="finicio.action_window"/>
    <menuitem name="Server to list" id="finicio" parent="finicio.menu_2"
              action="finicio.action_server"/>
    -->
  </data>
</odoo>